# Learning Rust
This Repository is following the Rust lecture I am taking starting in April 2026.

## How to build
To build the exercises simply `cd` into one folder and then use `cargo build` or `cargo build --release`. 
